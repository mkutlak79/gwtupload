<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>com.googlecode.gwtupload</groupId>
  <artifactId>gwtupload-project</artifactId>
  <name>GwtUpload project</name>
  <packaging>pom</packaging>
  <version>1.0.4-SNAPSHOT</version>

  <modules>
    <module>core</module>
  </modules>

  <parent>
    <groupId>org.sonatype.oss</groupId>
    <artifactId>oss-parent</artifactId>
    <version>7</version>
  </parent>

  <properties>
    <main.basedir>${project.parent.basedir}</main.basedir>
<!--    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>-->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <gwtVersion>2.12.0</gwtVersion>
    <gwtMaven>2.12.0</gwtMaven>
    <commonsFileuploadVersion>2.0.0-M2</commonsFileuploadVersion>
    <commonsIoVersion>2.18.0</commonsIoVersion>
    <gwtChismesVersion>0.9.8</gwtChismesVersion>
    <gwtExporterVersion>2.5.1</gwtExporterVersion>
    <gwtIncubatorVersion>1.5-Dec_28</gwtIncubatorVersion>
    <junitVersion>3.8.2</junitVersion>
    <log4jVersion>1.2.16</log4jVersion>
    <commonsLang3Version>3.1</commonsLang3Version>
    <downloadLabel>Beta</downloadLabel>
    <localMavenRepo>file:${env.HOME}/workspace/GWTUpload-mavenrepo</localMavenRepo>
    <sonatypeRepoId>sonatype-nexus-snapshots</sonatypeRepoId>
    <sonatypeUrl>https://oss.sonatype.org/content/repositories/snapshots</sonatypeUrl>
    <wikiSvnExec>true</wikiSvnExec>
  </properties>

  <distributionManagement>
    <snapshotRepository>
      <id>sonatype-nexus-snapshots</id>
      <url>https://oss.sonatype.org/content/repositories/snapshots</url>
      <uniqueVersion>false</uniqueVersion>
    </snapshotRepository>
  </distributionManagement>

  <profiles>
     <profile>
       <id>2.0.4</id>
       <properties>
         <gwtVersion>2.0.4</gwtVersion>
         <gwtMaven>1.3-SNAPSHOT</gwtMaven>
       </properties>
     </profile>
     <profile>
       <id>2.2.0</id>
       <properties>
         <gwtVersion>2.2.0</gwtVersion>
         <gwtMaven>2.1.0-1</gwtMaven>
       </properties>
       <dependencies>
       <dependency>
         <groupId>org.gwtproject</groupId>
         <artifactId>gwt-dev</artifactId>
         <version>${gwtVersion}</version>
         <scope>provided</scope>
       </dependency>
       </dependencies>
     </profile>
     <profile>
       <id>prod</id>
       <properties>
        <sonatypeRepoId>sonatype-nexus-staging</sonatypeRepoId>
        <sonatypeUrl>https://oss.sonatype.org/service/local/staging/deploy/maven2</sonatypeUrl>
        <wikiSvnExec>git</wikiSvnExec>
       </properties>
     </profile>
  </profiles>

  <dependencies>
    <dependency>
      <groupId>org.gwtproject</groupId>
      <artifactId>gwt-user</artifactId>
      <version>${gwtVersion}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <version>${log4jVersion}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>${commonsIoVersion}</version>
    </dependency>
    <dependency>
      <groupId>gwtchismes</groupId>
      <artifactId>gwtchismes</artifactId>
      <version>${gwtChismesVersion}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.google.gwt</groupId>
      <artifactId>gwt-incubator</artifactId>
      <version>${gwtIncubatorVersion}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.timepedia.exporter</groupId>
      <artifactId>gwtexporter</artifactId>
      <version>${gwtExporterVersion}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <description>
   GWTUpload is a library for uploading files to web servers,
   showing a progress bar with real information about the process (file size, bytes transferred, etc).
   It uses ajax requests to ask the web server for the upload progress.
   It has two components written in java, the server side with servlet and utility classes,
   and the client side that is compiled into javascript using gwt.

   GWTUpload-gae is a library with aditional classes needed for using GwtUpload in Google Application Engine (GAE).

   JSUpload is the client library but compiled and exported into javascript,
   so users without Gwt skills can use it directly in their web pages.

   It includes a bunch of useful examples to learn how to use the library and see its features.
  </description>

  <url>https://code.google.com/p/gwtupload/</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>manolo</id>
      <name>Manuel Carrasco Moñino</name>
      <email>manolo@apache.org</email>
      <organization>ASF</organization>
      <roles>
         <role>Owner</role>
         <role>Developer</role>
      </roles>
    </developer>
  </developers>

  <ciManagement />

  <mailingLists>
    <mailingList>
      <name>gwtupload@googlegroups.com</name>
      <post>gwtupload@googlegroups.com</post>
      <archive>https://groups.google.com/group/gwtupload</archive>
    </mailingList>
  </mailingLists>

  <repositories>
    <repository>
      <id>repo1</id>
      <url>https://repo1.maven.org/maven2/</url>
    </repository>
    <repository>
      <id>gwtupload</id>
      <url>https://gwtupload.googlecode.com/svn/mavenrepo/</url>
    </repository>
    <repository>
      <id>gwtchismes</id>
      <url>https://gwtchismes.googlecode.com/svn/mavenrepo/</url>
    </repository>
    <repository>
      <id>timepedia</id>
      <url>https://timefire-repository.googlecode.com/svn/mavenrepo/</url>
    </repository>
    <repository>
      <id>seasar</id>
      <url>https://maven.seasar.org/maven2</url>
    </repository>
    <repository>
      <id>gquery-plugins</id>
      <url>https://gwtquery-plugins.googlecode.com/svn/mavenrepo</url>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
	<repository>
	  <id>logicaldoc</id>
	  <url>https://logicaldoc.sourceforge.net/maven/</url>
	</repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>gwtupload</id>
      <url>https://gwtupload.googlecode.com/svn/mavenrepo/</url>
    </pluginRepository>
    <pluginRepository>
      <id>codehaus.snapshots</id>
      <url>https://snapshots.repository.codehaus.org/</url>
    </pluginRepository>
  </pluginRepositories>

  <issueManagement>
    <system>Google Code</system>
    <url>https://code.google.com/p/gwtupload/issues/list</url>
  </issueManagement>
  <scm>
    <connection>scm:git:git@github.com:manolo/gwtupload.git</connection>
    <developerConnection>scm:git:git@github.com:manolo/gwtupload.git</developerConnection>
    <url>https://github.com/manolo/gwtupload</url>
  </scm>
<!--
  <distributionManagement>
    <repository>
      <uniqueVersion>false</uniqueVersion>
      <id>googlecode</id>
      <url>svn:https://gwtupload.googlecode.com/svn/mavenrepo/</url>
    </repository>
    <site>
      <id>googlecode</id>
      <url>svn:https://gwtupload.googlecode.com/svn/site</url>
    </site>
    <snapshotRepository>
      <uniqueVersion>false</uniqueVersion>
      <id>localfolder</id>
      <url>${localMavenRepo}</url>
    </snapshotRepository>
  </distributionManagement>
-->

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <configuration>
          <descriptors>
            <descriptor>${basedir}/assembly.xml</descriptor>
          </descriptors>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>2.8</version>
        <configuration>
          <excludePackageNames>com.google:*</excludePackageNames>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <configuration>
          <targetJdk>1.5</targetJdk>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <configuration>
          <configLocation>${main.basedir}/gwt-checkstyle.xml</configLocation>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
